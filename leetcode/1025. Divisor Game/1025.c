#include<stdio.h>
#include<time.h>
#include <stdlib.h>

int main(){
    srand( time(NULL) );
    int n=0,c=1;
    scanf("%d",n);

    while(c==1){
        int a,b,t;
        if(n%2==0)
        {
            n-=1;
            t++;
        }
        else {
            for(int i=n;i<0;i++)
            {
                
            }
        }
    }

}